# nextlat_hmm.yaml
#
# GENERATED by scripts/materialize_configs.py -- do not hand-edit.
# Regenerate:  .venv/bin/python scripts/materialize_configs.py
# Verify:      .venv/bin/python scripts/materialize_configs.py --check
#
# COPIED from the pinned official configuration, per spec section 8:
#   upstream commit 3770be6009cea2b3c455a9ce7f2ca88b504bb955
#   config/stargraph/5_5/nextlat_stargraph_5_5.yaml
#   sha256 6d98ffc681d6574640806f4c5224de466d09b1dc4f9e1e56e2fa6bdf6572a58d
# Every changed key is declared in configs/overrides.json with its spec authority, and
# docs/CONFIG_DEVIATIONS.md carries the prose. The generator refuses to emit a file in
# which an upstream key vanished, a value changed without a declaration, a hoisted key
# changed value, or a frozen key moved without a written spec authority.
#
# Launch (spec section 8, single GPU):
#   scripts/launch_train.sh nextlat_hmm.yaml <seed>
# which expands to
#   fabric run --devices 1 --precision bf16-mixed train.py --config <this file> \
#     seed=<seed> trainer.out_dir=... trainer.experiment_name=...
use_nextlat: true
trainer:
  epochs: -1
  train_batches: 3000
  val_batches: 200
  test_batches: 200
  val_only: false
  val_printsamples: false
  log_interval: 1
  val_interval: 300
  test_interval: 300
  out_dir: /content/lurestar/runs/hmm/nextlat/seed1234/base
  init_from: scratch
  wandb_project: hmm_belief
  compile: false
  experiment_name: nextlat-seed1234-hmm
  wandb_tags:
  - hmm
  - 4state4obs
  save_last_checkpoint: true
  save_best_checkpoint: true
  log_to_file: true
  save_recovery_checkpoint: 250
  log_to_wandb: false
data:
  dataset: hmm_belief
  effective_batch_size: 256
  gradient_accum_steps: 1
  pair_accum_steps: 1
  num_workers: 0
  test_generalization: false
  hmm:
    hmm_train_data_path: /content/lurestar/data/hmm/hmm-train-100000-len32.npz
    hmm_val_data_path: /content/lurestar/data/hmm/hmm-val-10000-len32.npz
    hmm_generalization_data_path: /content/lurestar/data/hmm/hmm-gen-10000-len64.npz
    hmm_matrices_path: /content/lurestar/manifests/hmm-matrices.json
    hmm_num_states: 4
    hmm_num_observations: 4
    train_sequences: 100000
    val_sequences: 10000
    generalization_sequences: 10000
    sequence_length: 32
    generalization_sequence_length: 64
model:
  n_layer: 4
  n_head: 4
  n_embd: 128
  dropout: 0.0
  bias: false
  vocab_size: 0
  block_size: 1024
  context_length: 0
  gpt_mode: next_token
  lambda_kl: 0.0
  mtp_horizon: 1
  lambda_mse: 1.0
  compute_hidden_state_rank: false
  proj_factor: 0.5
  lambda_ce: 0.0
optimizer:
  optimizer_type: adam
  learning_rate: 5e-4
  weight_decay: 0.1
  beta1: 0.9
  beta2: 0.95
  grad_clip: 100
lr_scheduler:
  warmup_iters: 0
  warmdown_iters: 0
seed: 1234
provenance:
  upstream_repo: JaydenTeoh/NextLat
  upstream_commit: 3770be6009cea2b3c455a9ce7f2ca88b504bb955
  source_config: config/stargraph/5_5/nextlat_stargraph_5_5.yaml
  source_config_sha256: 6d98ffc681d6574640806f4c5224de466d09b1dc4f9e1e56e2fa6bdf6572a58d
  generator: scripts/materialize_configs.py
  note: Spec sec.12 required experiment B. Copied from the official Path-Star G(5,5) YAML so that every
    key the generic trainer reads unconditionally (trainer.test_interval, trainer.val_printsamples, data.test_generalization,
    ...) is present; only the keys spec sec.12 names are changed.
preregistration:
  confirmatory_seeds:
  - 1234
  - 1235
  - 1236
  primary_distance: centered cosine
  robustness_distance: whitened Euclidean
