# gpt_hmm.yaml
#
# GENERATED by scripts/materialize_configs.py -- do not hand-edit.
# Regenerate:  .venv/bin/python scripts/materialize_configs.py
# Verify:      .venv/bin/python scripts/materialize_configs.py --check
#
# COPIED from the pinned official configuration, per spec section 8:
#   upstream commit 3770be6009cea2b3c455a9ce7f2ca88b504bb955
#   config/stargraph/5_5/gpt_stargraph_5_5.yaml
#   sha256 82b31f83f2a9b76077bfbf3d9fd7ec7ae467012161477527c7b2d8db01392cb7
# Every changed key is declared in configs/overrides.json with its spec authority, and
# docs/CONFIG_DEVIATIONS.md carries the prose. The generator refuses to emit a file in
# which an upstream key vanished, a value changed without a declaration, a hoisted key
# changed value, or a frozen key moved without a written spec authority.
#
# Launch (spec section 8, single GPU):
#   scripts/launch_train.sh gpt_hmm.yaml <seed>
# which expands to
#   fabric run --devices 1 --precision bf16-mixed /content/lurestar/scripts/train_hmm.py --config <this file> \
#     seed=<seed> trainer.out_dir=... trainer.experiment_name=...
use_bst: false
use_nextlat: false
trainer:
  epochs: -1
  train_batches: 3000
  val_batches: 200
  test_batches: 200
  val_only: false
  val_printsamples: false
  log_interval: 1
  val_interval: 300
  test_interval: 300
  out_dir: /content/lurestar/runs/hmm/gpt/seed1234/base
  init_from: scratch
  wandb_project: hmm_belief
  compile: false
  experiment_name: gpt-seed1234-hmm
  wandb_tags:
  - hmm
  - 4state4obs
  save_last_checkpoint: true
  save_best_checkpoint: true
  log_to_file: true
  save_recovery_checkpoint: 250
  log_to_wandb: false
data:
  dataset: hmm_belief
  effective_batch_size: 256
  gradient_accum_steps: 1
  pair_accum_steps: 1
  num_workers: 0
  test_generalization: true
  hmm_train_data_path: /content/lurestar/data/hmm/hmm4x4_train_len32_100000.npy
  hmm_val_data_path: /content/lurestar/data/hmm/hmm4x4_val_len32_10000.npy
  hmm_generalization_data_path:
  - /content/lurestar/data/hmm/hmm4x4_lengen_len64_10000.npy
  hmm_n_obs: 4
  hmm_matrices_path: /content/lurestar/manifests/hmm_matrices.json
  hmm_num_states: 4
  hmm_train_sequences: 100000
  hmm_val_sequences: 10000
  hmm_generalization_sequences: 10000
  hmm_sequence_length: 32
  hmm_generalization_sequence_length: 64
model:
  n_layer: 4
  n_head: 4
  n_embd: 128
  dropout: 0.0
  bias: false
  vocab_size: 0
  block_size: 1024
  context_length: 0
  gpt_mode: next_token
  compute_hidden_state_rank: false
optimizer:
  optimizer_type: adam
  learning_rate: 5e-4
  weight_decay: 0.1
  beta1: 0.9
  beta2: 0.95
  grad_clip: 100
lr_scheduler:
  warmup_iters: 0
  warmdown_iters: 0
seed: 1234
provenance:
  upstream_repo: JaydenTeoh/NextLat
  upstream_commit: 3770be6009cea2b3c455a9ce7f2ca88b504bb955
  source_config: config/stargraph/5_5/gpt_stargraph_5_5.yaml
  source_config_sha256: 82b31f83f2a9b76077bfbf3d9fd7ec7ae467012161477527c7b2d8db01392cb7
  generator: scripts/materialize_configs.py
  note: Spec sec.12 required experiment B. Copied from the official Path-Star G(5,5) YAML so that every
    key the generic trainer reads unconditionally (trainer.test_interval, trainer.val_printsamples, data.test_generalization,
    ...) is present; only the keys spec sec.12 names are changed.
preregistration:
  confirmatory_seeds:
  - 1234
  - 1235
  - 1236
  - 1237
  - 1238
  primary_distance: centered cosine
  robustness_distance: whitened Euclidean
